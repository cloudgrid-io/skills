# multi-service

A static frontend (`web`) and a Node.js API backend (`api`) sharing a
grid-managed MongoDB. The generic multi-service skeleton for CloudGrid.

See `index.md` for the template reference and key rules.
